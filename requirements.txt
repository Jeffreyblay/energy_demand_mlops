# GridVision — Phase 1 (local prototype) dependencies.
requests>=2.31
python-dotenv>=1.0
pandas>=2.1
pyarrow>=14.0          # parquet engine
numpy>=1.26
lightgbm>=4.1
scikit-learn>=1.3      # metrics (MAPE/RMSE), train utilities
holidays>=0.40         # US federal holiday flags
mlflow>=2.14           # experiment tracking + model registry (alias API)
sqlalchemy>=2.0        # Postgres access (src/db.py)
psycopg2-binary>=2.9   # Postgres driver
fastapi>=0.115         # forecast-serving API (Phase 5)
uvicorn[standard]>=0.30  # ASGI server to run the FastAPI app